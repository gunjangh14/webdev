// This models will hold the response to API LogIn request 

var app = app || {}

app.UserProfile = new Backbone.Model.extend({
	// For now the model is empty because we are just passing the full response of the call
	
});